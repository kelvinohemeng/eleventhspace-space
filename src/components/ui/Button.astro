---
import { ArrowUpRight } from "@phosphor-icons/react";
import Magnet from "../react/reactbits/Magnet";

const { title = "Learn more", href = "/" } = Astro.props;

let text = "text";
---

<Magnet client:visible padding={20} disabled={false} magnetStrength={10}>
  <a href={href}>
    <button
      class="text-e-lg text-white bg-black full flex gap-3 items-center px-6 py-4 rounded-full"
    >
      <span>{title}</span>
      <ArrowUpRight /></button
    >
  </a>
</Magnet>

<script define:vars={{ text }}>
  import { gsap } from "gsap";
  text = "00Title";

  console.log(text);
</script>
